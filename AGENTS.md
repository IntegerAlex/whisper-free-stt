## Agent skills

### Issue tracker

Issues and specs for this repo live as GitHub issues (remote `origin` → `github.com/IntegerAlex/whisper-free-stt`). Uses the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` at the repo root, ADRs under `docs/adr/`. See `docs/agents/domain.md`.
